# MI300X Bootcamp — dependencies (April 2026)
#
# 1) ROCm PyTorch (system index), then core stack (single resolver — avoids resolution-too-deep):
#
#     pip install --index-url https://download.pytorch.org/whl/rocm6.2 torch
#     pip install -r requirements-core.txt
#
# 2) Optional extras — run as separate installs (do not merge into one requirements file):
#
#     pip install -r requirements-vllm.txt    # inference; CUDA-oriented on PyPI
#     pip install -r requirements-demo.txt  # Gradio
#
# If you prefer Unsloth's bundled ROCm path instead of a manual torch line:
#     pip install "unsloth[rocm]"
#
# This file only includes the core set so `pip install -r requirements.txt` stays resolvable.
-r requirements-core.txt
